<!-- The ORIGINAL problem, captured faithfully before any solutioning. This is the source of truth
     for "why". Keep the user's own framing; do not smuggle a solution in here. -->

# Problem — {{TASK_TITLE}}

## The problem, as stated

<!-- The user's raw description of the idea or pain, lightly cleaned up. Preserve their intent and wording. -->

## Whose problem it is

<!-- Name the affected party concretely: the operator running `worc`, an agent executing a node of a
     flow, or a maintainer of this repository. Different owners lead to different solutions. -->

## Current situation (the pain)

<!-- What happens today, and why it hurts. Concrete and observable: the command that is run, the
     state it leaves behind, the failure it produces. -->

## Why it matters now

<!-- The trigger, the cost of leaving it alone: wasted tokens or money on a run, a task stuck in
     `manual_action_required`, a gate that cannot be trusted, an operator step nobody can repeat. -->

## Signals & evidence

<!-- Anything that grounds the problem in reality, with a pointer: a task run under `.worc/` (the
     ledger, a node log, the prompt audit), a row in `state.db`, a post-mortem, a diff, an operator
     report. Cite `path:line` where the code itself is the evidence. -->

## Constraints given up front

<!-- Hard boundaries already imposed: greenfield MVP (no deployed installs to migrate), cross-platform
     Windows/Linux/macOS, the security envelope, "must reuse X", a deadline, a provider limitation. -->
