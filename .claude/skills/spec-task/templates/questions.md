<!-- LIVING document. An open question is logged here the moment it surfaces, in any document, and its
     answer moves to Resolved — never deleted, because the decision trail is the point. A blocking
     question must be resolved before the plan is signed off. -->

# Open questions — {{TASK_TITLE}}

## Open

| #   | Question     | Blocking? | Raised in | Owner       |
| --- | ------------ | --------- | --------- | ----------- |
| Q-1 | `<question>` | yes / no  | `<doc>`   | user / spec |

## Resolved

| #   | Question | Answer | Decided on |
| --- | -------- | ------ | ---------- |
|     |          |        |            |

<!-- "Blocking?" means it must be answered before implementation starts. While one blocking question
     is open, the folder's Status is not `ready-to-implement`.

     Verify what can be verified instead of asking: an assumption about a config key, a node id, a
     status name or a provider flag is answered by reading the code, and only a genuine product or
     trade-off decision belongs to the user. -->
